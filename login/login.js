const queryString = require('query-string');

function efetuarLogin(login, password) {
    loginBruto = document.querySelector("#loginInput")
    login = loginBruto.value
    console.log(login)
    passwordBruto = document.querySelector("#passwordInput")
    password = passwordBruto.value
    console.log(password)
};